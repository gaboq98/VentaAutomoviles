@model IEnumerable<VentaAutomoviles.Models.Direccion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Señas)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Canton.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pais.Iso)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Provincia.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Señas)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Canton.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pais.Iso)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Provincia.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdDireccion }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdDireccion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdDireccion })
        </td>
    </tr>
}

</table>
